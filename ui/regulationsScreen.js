import { getCategory,pageHead } from "./helpers.js";
export function renderRegulationsScreen(world,data){const category=getCategory(world),rules=category.raceRules;return`${pageHead("Reglamentos",`${category.name} · Configuración data-driven`)}<div class="grid two"><article class="card"><div class="metric-label">SISTEMA DE PUNTOS</div><div class="points-grid">${category.pointsSystem.map((points,index)=>`<div><span>P${index+1}</span><strong>${points}</strong></div>`).join("")}</div></article><article class="card regulation-list"><div class="metric-label">FORMATO DEPORTIVO</div><div><span>Equipos</span><strong>${category.teams}</strong></div><div><span>Pilotos por equipo</span><strong>${category.driversPerTeam}</strong></div><div><span>Rondas</span><strong>${category.calendar.length}</strong></div><div><span>Riesgo base de abandono</span><strong>${Math.round(rules.baseReliabilityRisk*100)}%</strong></div><div><span>Variabilidad</span><strong>${rules.randomness}/10</strong></div></article></div><article class="card" style="margin-top:16px"><div class="metric-label">PONDERACIÓN DE SIMULACIÓN</div><div class="weight-grid">${Object.entries(rules.weights).map(([key,value])=>`<div><span>${key}</span><strong>${Math.round(value*100)}%</strong></div>`).join("")}</div></article>`;}
